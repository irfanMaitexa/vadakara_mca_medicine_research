import 'package:flutter/material.dart';

class StaffViewOrderScreen extends StatelessWidget {
  const StaffViewOrderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: Text('Staff order Screen'),
      ),
      body: Column(),
    );
  }
}